import { View, Text } from 'react-native';
export default function PrintScreen({ navigation }) {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Ini Halaman Print Thermal</Text>
        </View>
    );
}